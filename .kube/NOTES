Add New Cluster
---------------
awsprf alprscom

# get cluster name
aws eks list-clusters --query "clusters[][@]" --output text

# get admin role ARN
aws iam list-roles --query "Roles[?contains(RoleName,'EKSAdminRole')].Arn" --output text

# update ~/.kube/config
aws eks update-kubeconfig --name alprs --role-arn arn:aws:iam::513953033057:role/ALPRSEKSAdminRole

# sanity check
kubectl get all

# RENAME context using friendly name
kubectl config rename-context `kubectl config current-context` alprsdev

